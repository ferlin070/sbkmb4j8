{
  "name": "candle-collection-log",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A cozy single-page app to catalog every candle you own or have burned through.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5",
    "jsdom": "^24.1.1"
  }
}
